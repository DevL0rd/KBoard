add_library(KBoardConfig SHARED settingsbridge.cpp settingsbridge.h)
kconfig_add_kcfg_files(KBoardConfig GENERATE_MOC kboardsettings.kcfgc)
target_include_directories(KBoardConfig PUBLIC ${CMAKE_CURRENT_SOURCE_DIR} ${CMAKE_CURRENT_BINARY_DIR})
target_link_libraries(KBoardConfig PUBLIC Qt::Core Qt::Gui Qt::Qml KF6::ConfigCore KF6::ConfigGui)
set_target_properties(KBoardConfig PROPERTIES VERSION ${PROJECT_VERSION} SOVERSION 0)
target_compile_options(KBoardConfig PRIVATE -fvisibility=default)
ecm_add_qml_module(KBoardConfig URI "org.devl0rd.kboard.config" GENERATE_PLUGIN_SOURCE DEPENDENCIES QtQuick)
ecm_finalize_qml_module(KBoardConfig DESTINATION ${KDE_INSTALL_QMLDIR})
install(TARGETS KBoardConfig ${KDE_INSTALL_TARGETS_DEFAULT_ARGS} LIBRARY NAMELINK_SKIP)
install(FILES kboardsettings.kcfg DESTINATION ${KDE_INSTALL_KCFGDIR})
